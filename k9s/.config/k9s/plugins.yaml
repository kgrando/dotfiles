plugins:
  lnav:
    shortCut: Ctrl-Y
    confirm: false
    description: "lnav"
    scopes:
      - pods
    command: bash
    background: false
    args:
      - -c
      - |
        kubectl logs -f -n $NAMESPACE $NAME --all-containers | lnav
